;;; tree-sitter.el --- Emacs bindings to the tree-sitter parser for c  -*- lexical-binding: t; -*-

;;;###autoload
(autoload 'tree-sitter-lang-c "tree-sitter-lang-c-module"
  "Return the tree-sitter language object for c.

(fn)")

(provide 'tree-sitter-lang-c)
